
app.registerCtrl('consultaAluno', function($scope, $http, $location, $routeParams) {
/*
	$scope.alunos = [];
	$scope.pessoas = [];
	$scope.table = false;

	$scope.getPessoa = function(item, model, label){
		$scope.alunos = {};
		$http.get('../api/index.php/pessoa/' + item.id).    
		success(function(data, status, headers, config) {
			if(data.error == 0){
				console.log("RESULTADO DA BUSCA",data);
				$scope.table = true;
				$scope.alunos = data.pessoa;
			}
		}).
		error(function(data, status, headers, config) {
        // log error
    	}); 
	}

	$scope.getMatricula = function(item, model, label){
		$scope.alunos = {};
		$http.get('../api/index.php/pessoa/' + item.id).    
		success(function(data, status, headers, config) {
			if(data.error == 0){
				console.log("RESULTADO DA MATRICULA",data);
				$scope.table = true;
				$scope.alunos = data.pessoa;
			}
		}).
		error(function(data, status, headers, config) {
        // log error
    	}); 
	}
*/
});